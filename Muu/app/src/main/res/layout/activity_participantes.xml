<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollView1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="10dp"
    android:paddingRight="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Creando vaca"
            android:textAppearance="?android:attr/textAppearanceLarge" />
        <Button
            android:id="@+id/button1Contacto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="mostrarSeleccionContactos"
            android:text="Seleccionar Participantes" />

        <Button
            android:id="@+id/button1Calendario"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="mostrarSeleccionCalendario"
            android:text="Seleccionar Fecha" />

        <ListView
            android:id="@+id/listaParticipantes"
            android:layout_width="match_parent"
            android:layout_height="391dp"
            android:padding="10dp"
            android:textSize="20sp">

        </ListView>

        <Button
            android:id="@+id/finVaca"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="vacaFin"
            android:text="Finalizar Vaca" />
    </LinearLayout>
</ScrollView>